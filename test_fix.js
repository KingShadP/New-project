const assert = require('assert');

// The failure is clearly `npx shopify hydrogen deploy` but we don't have hydrogen installed in our project or any hydrogen code!
console.log('We should remove the hydrogen deployment script if this is not a hydrogen project.')
